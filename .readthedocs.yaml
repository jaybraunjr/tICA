version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre_build:
      - python -m pip install -r docs/requirements.txt

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
